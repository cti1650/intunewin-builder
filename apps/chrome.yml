name: chrome

download:
  url: https://dl.google.com/chrome/install/googlechromestandaloneenterprise64.msi
  file: googlechromestandaloneenterprise64.msi

installer:
  type: msi
  install_args: "/i {installer} /qn /norestart"

detect:
  file: "C:\\Program Files\\Google\\Chrome\\Application\\chrome.exe"
  # Chrome Enterprise MSI は consumer stable channel から 1〜2 マイナー遅延する。
  # 2026-05-10 時点で MSI 配布版は 147.0.7727.138 (consumer stable は 148.0.7778.97)。
  version: "147.0.7727.138"
  registry_display_name: "Google Chrome"

uninstall:
  type: msi
  args: "/x {product_code} /qn"

# ==========
# Intune Win32アプリ設定値（参照用）
# ==========
intune:
  install_behavior: system
  install_command: "msiexec /i googlechromestandaloneenterprise64.msi /qn /norestart"
  # ProductCodeはMSIのプロパティまたは検証時のレジストリから確認
  uninstall_command: 'msiexec /x "{ProductCode}" /qn'
  detection:
    type: file
    path: "C:\\Program Files\\Google\\Chrome\\Application"
    file: "chrome.exe"
