name: slack

download:
  url: https://slack.com/api/desktop.latestRelease?arch=x64&variant=msix&redirect=true
  file: slack.msix

installer:
  type: msix

detect:
  appx_name: "Slack"

uninstall:
  type: msix
  package_name: "Slack"
